{
  "name": "model-architectures-explainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "node scripts/check_data.mjs",
    "check:links": "node scripts/check_links.mjs",
    "test": "node --test 'test/**/*.test.mjs'"
  },
  "dependencies": {
    "@astrojs/svelte": "^7.2.5",
    "astro": "^5.0.0",
    "svelte": "^5.56.3"
  }
}
